
name: Presubmit

on:
  pull_request:
    branches: [main]

jobs:
  Kotlin-Lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-java@v3
      with:
        distribution: "temurin"
        java-version: '11'
        cache: "gradle"


    - name: ktlintCheck
      shell: bash
      run: ./gradlew ktlint
  # Rust-Lint:
  #   runs-on: ubuntu-latest
  #   steps:


